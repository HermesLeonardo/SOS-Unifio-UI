
  # Sistema de Emergência Médica UNIFIO

  This is a code bundle for Sistema de Emergência Médica UNIFIO. The original project is available at https://www.figma.com/design/tyJkISVhaYKJEE5FwJwAYR/Sistema-de-Emerg%C3%AAncia-M%C3%A9dica-UNIFIO.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
  run `npm install socket.io-client` tmb